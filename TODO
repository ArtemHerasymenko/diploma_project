1. Generate dataset (images,sounds, images + sounds)
2. Train model only on images(to make sure adding audio improves precision)
3. Write model which will be able to work with images, sounds, images + sounds.
